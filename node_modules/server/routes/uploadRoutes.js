import { Router } from 'express';
import { upload, uploadResume, parseLinkedIn } from '../controllers/uploadController.js';

const router = Router();
router.post('/resume', upload.single('file'), uploadResume);      // POST /api/upload/resume
router.post('/linkedin', upload.single('file'), parseLinkedIn);   // POST /api/upload/linkedin
export default router;
